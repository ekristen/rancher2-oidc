apiVersion: v1
kind: Service
metadata:
  name: {{ include "oidc-aggregator.fullname" . }}
  labels:
    {{- include "oidc-aggregator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "oidc-aggregator.selectorLabels" . | nindent 4 }}
