apiVersion: v1
kind: Service
metadata:
  name: {{ include "rancher2-oidc.fullname" . }}
  labels:
    {{- include "rancher2-oidc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rancher2-oidc.selectorLabels" . | nindent 4 }}
